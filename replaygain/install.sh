#!/bin/bash

echo "Installing ReplayGain plugin"

# Nothing to install: the plugin only contributes settings to mpd.conf through
# MPD's own ReplayGain support, which is already built into Volumio's MPD.

echo "plugininstallend"
